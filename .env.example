# Base URL of the standalone AaspaasBazaarBackend service.
# The admin app is a pure frontend: every CRUD operation is proxied to this
# service (the only writer to Firestore). No Firebase credentials live here.
API_BASE_URL=http://localhost:8080
